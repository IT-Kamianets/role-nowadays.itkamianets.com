import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.itkamianets.rolenow',
  appName: 'Role Nowadays',
  webDir: 'dist/role-nowadays/browser'
};

export default config;
